# PATTERN_TEST_DOC.md

> **Назначение:**
> Документация по тесту на паттерны поведения.
> Содержит пошаговую инструкцию пользователя, описание логики теста, форматы данных, обзор компонентов и сценариев, визуальные схемы, таблицы для быстрого восприятия.

---

## 📑 Оглавление

1. [Требования к окружению и запуску](#требования-к-окружению-и-запуску)
2. [Общее описание](#общее-описание)
3. [Варианты прохождения теста](#варианты-прохождения-теста)
      - [Вариант 1: На сайте](#вариант-1-прохождение-теста-на-сайте)
      - [Вариант 2: В Telegram-боте](#вариант-2-прохождение-теста-через-telegram-бота)
4. [Структура результатов](#структура-и-интерфейс-результатов)
5. [Логика расчёта результатов](#логика-расчёта-результатов)
6. [Данные и форматы](#данные-и-форматы)
7. [Требования к стилям и кастомизации](#требования-к-стилям-и-кастомизации)
8. [Генерация PDF-отчёта](#генерация-pdf-отчёта)
9. [Расширяемость и поддержка](#расширяемость-и-поддержка)
10. [Пошаговый разбор работы приложения](#пошаговый-разбор-работы-приложения)
11. [Таблицы компонентов, данных, сценариев](#таблицы-компонентов-данных-сценариев)
12. [Схемы и последовательности](#схемы-и-последовательности)
13. [Дополнительная информация](#дополнительная-информация)

---

## Требования к окружению и запуску

- Node.js 18+
- React 18+
- Установка: `npm install`
- Запуск разработки: `npm start`
- Сборка: `npm run build`
- Статичные данные (JSON, изображения) размещаются в `public/data` и `public/img`
- Тестовый сервер: `node server.js` — http://localhost:5000

---

## Общее описание

Тест на паттерны поведения — интерактивный инструмент для диагностики ключевых моделей реагирования, мышления, общения и поведения. Подходит для личного развития, самоанализа и повышения эффективности коммуникации.

---

## Варианты прохождения теста

### Вариант 1: Прохождение теста на сайте

- Пользователь проходит тест на сайте.
- Последовательность экранов:
     1. **Введение** — описание целей теста, кнопка "Начать тест"
     2. **Ввод данных** — форма для ФИО, контактов, согласий
     3. **Вопросы теста** — пошаговое прохождение с выбором вариантов
     4. **Результаты теста** — визуализация, интерпретация, PDF
- Все ответы и результаты обрабатываются на клиенте.

### Вариант 2: Прохождение теста через Telegram-бота (`PatternsTest`)

- Пользователь проходит тест в Telegram-боте.
- Вопросы приходят в чат, ответы отправляются боту.
- После теста бот отправляет ответы на сервер, где они сохраняются в базу данных.
- Сервер формирует индивидуальную ссылку для просмотра (например, `https://site.ru/results/1234`).
- В браузере отображается подробный анализ, визуализация, PDF-отчёт.

---

## Структура и интерфейс результатов

- **Блок 1:** Приветствие и рекомендации, кнопка "Скачать PDF"
- **Блок 2:** Результаты тестирования — гистограммы, явные паттерны, ведущая категория
- **Блок 3:** Интерпретация — ведущая категория, топ-паттерны, тип реагирования, модель, сильные стороны, комментарий
- **Блок 4:** Литература — рекомендованные источники
- **Блок 5:** Подробное распределение паттернов по категориям и подкатегориям

---

## Логика расчёта результатов

- Результаты рассчитываются по структуре категорий и подкатегорий паттернов (`patterns.json`).
- Для каждой подкатегории:
  `Процент = (Число выборов паттерна / Общее число выборов в подкатегории) * 100`
- Явно проявленный паттерн — выраженность 75% и выше.
- Логика реализована в `utils/createResultsData.js`.

---

## Данные и форматы

| Тип данных     | Описание                                   | Пример                                   |
| -------------- | ------------------------------------------ | ---------------------------------------- |
| answers        | Массив выбранных паттернов                 | ["Дефицит времени", ...]                 |
| patterns.json  | Категории, подкатегории, паттерны, стили   | { "category": "...", "patterns": [...] } |
| questions.json | Вопросы, варианты, сопоставление паттернов | { "question": "...", "patterns": [...] } |

- Все данные — JSON, UTF-8
- Ответы пользователя — массив строк, сохраняется на сервере

---

## Требования к стилям и кастомизации

- Индивидуальные CSS-классы для категорий, подкатегорий и паттернов
- Лидер-категории и явные паттерны выделяются стилями
- Форма и кнопки стилизованы по макетам

---

## Генерация PDF-отчёта

- PDF-отчёт доступен на странице результатов
- Генерация — через `utils/pdfmakeGenerator.js`
- Структура блоков PDF — см. [PATTERN_TEST_PDF.md](PATTERN_TEST_PDF.md)

---

## Расширяемость и поддержка

- Добавление новых вопросов, паттернов, категорий — редактировать соответствующие JSON-файлы
- Логика расчёта — в `utils/createResultsData.js` и `PatternBarChart.jsx`
- React-компоненты, легко расширяемые
- Валидация форм, обработка ошибок, покрытие комментариями

---

# Пошаговый разбор работы приложения

## Последовательность этапов (текстовая схема)

```
Пользователь
    ↓
[IntroScreen] → [FormScreen] → [QuestionsScreen] → [ResultsScreen]
    ↓
Сервер (сохраняет ответы)
    ↓
[PDF-отчёт, визуализация]
```

---

## 1. Страницы и порядок работы приложения

| Страница     | Компонент/Маршрут          | Назначение                                    |
| ------------ | -------------------------- | --------------------------------------------- |
| Главная      | `/`, IntroScreen           | Приветствие, вводная, инструкция, старт теста |
| Тестирование | `/test`, TestPage          | Форма → вопросы → результаты                  |
| Результаты   | `/results/:id`, ResultPage | Просмотр результата по уникальной ссылке      |

---

## 2. Рабочая логика для ответов (answers)

- **answers** — массив строк, соответствующих названиям паттернов из `patterns.json`
- Совпадение названия важно для корректного расчёта и визуализации

---

## 3. Как работает на практике?

**Пример вопроса из questions.json:**

```json
{
	"question": "Выберите из представленных слов то, которое больше нравится:",
	"options": ["Быстро", "Обдуманно"],
	"patterns": ["Дефицит времени", "Изобилие времени"]
}
```

---

## Таблицы компонентов, данных, сценариев

### Основные компоненты

| Компонент       | Назначение                        | Входные данные          | Выходные данные   |
| --------------- | --------------------------------- | ----------------------- | ----------------- |
| IntroScreen     | Приветствие, вводная, старт теста | -                       | -                 |
| FormScreen      | Форма пользователя                | userData                | userData          |
| QuestionsScreen | Пошаговые вопросы                 | questions.json          | answers (массив)  |
| ResultsScreen   | Итоговые результаты               | patternResults, answers | Визуализация, PDF |
| PatternBarChart | Гистограмма паттернов             | patternResults          | График            |

---

### Сценарии пользователя

| Сценарий            | Шаги                                | Результат              |
| ------------------- | ----------------------------------- | ---------------------- |
| На сайте            | Интро → форма → вопросы → результат | PDF, уникальная ссылка |
| Через Telegram-бота | Бот → ответы → сервер → ссылка      | PDF, уникальная ссылка |

---

## Схемы и последовательности

### Текстовая схема архитектуры

```
[Frontend: React]
  ├─ IntroScreen
  ├─ FormScreen
  ├─ QuestionsScreen
  └─ ResultsScreen
         │
         ▼
[Utils: createResultsData.js, pdfmakeGenerator.js]
         │
         ▼
[Backend: server.js]
         │
         ▼
[Database (опционально)]
```

---

## Дополнительная информация

- Проект поддерживает валидацию форм и обработку ошибок на каждом этапе
- Порядок вопросов и вариантов перемешивается для повышения валидности
- Основные алгоритмы расчётов — отдельные модули
- Ответы из Telegram-бота автоматически обрабатываются сервером

---

## Кратко по шагам пользователя

1. Заходит на `/pattern-test`
2. Вводная, "Начать тест"
3. Форма (ФИО, контакты, согласие)
4. Вопросы пошагово
5. Отправка на сервер
6. Сервер возвращает ID результата
7. Страница результата или ссылка на `/pattern-test/results/:id`
8. Доступно из письма, Telegram и т.д.

---

## Прохождение теста через Telegram-бота

1. Тест в чате Telegram-бота
2. Ответы на сервер, возвращается уникальный id
3. Бот присылает ссылку `https://ai4g.ru/pattern-test/results/:id`
4. Открываешь ссылку — тот же интерфейс, что и сайт
